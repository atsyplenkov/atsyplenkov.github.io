name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

# Build and audit only. GitHub Pages deployment stays inactive until the
# approved root-repository handoff (see docs/migration/freeze/README.md).
permissions:
  contents: read

jobs:
  accept:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: typst-community/setup-typst@v4
      - uses: astral-sh/setup-uv@v7
      - name: Clean build and black-box audit
        run: uv run scripts/accept.py
